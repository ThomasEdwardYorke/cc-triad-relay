node_modules/
*.log
.DS_Store
.vscode/
.idea/
coverage/
.nyc_output/
tmp/
*.tsbuildinfo
.env
.env.local
.env.*.local

# test runtime
.claude-test/

# Do NOT ignore core/dist/ (we commit the built artifact for zero-install UX)
# But do ignore intermediate build files
*.js.map.orig

# Local-only self-hosting harness config and notes.
# Keep live repository-development state out of public main.
/harness.config.json
harness.config.json.local
/docs/maintainer/handoff/

# Local-only design notes and handoff state (not shipped with the plugin)
.docs/
# Exception: handoff template ships with the plugin so consumers can `harness init`
# the 4-layer handoff structure (current / backlog / decisions / roadmap).
!template/.docs/
!template/.docs/**
