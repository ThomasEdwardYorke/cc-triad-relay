# History — {{PROJECT_NAME}}

> **Handoff-mode default project**: This file holds **completion history**. The dispatch source for new active tasks is `.docs/handoff/{{PROJECT_NAME}}-backlog.md`, the Phase / Week / Task SSoT is `.docs/handoff/{{PROJECT_NAME}}-roadmap.md`, design decisions are in `.docs/handoff/{{PROJECT_NAME}}-decisions.md`, and the current state index is `.docs/handoff/{{PROJECT_NAME}}-current.md`.
>
> **Active task append is prohibited** in this file. The only allowed append target is the **PR-merge completion log** in the `## Completed` section below.

This file preserves the completion history for each task in {{PROJECT_NAME}}. Labels: `[feature]` `[fix]` `[improve]` `[test]` `[docs]` `[refactor]` `[security]` `[infra]`

See `CLAUDE.md` for the project specification and `CLAUDE.local.md` (personal local, gitignored) for workflow conventions.

---

## Completed

(Append on each PR merge — record one entry per Round / Phase / Task.)

### Round 1 / Phase 1 (Date: YYYY-MM-DD)

(Example entry)

- ✅ T-001 [feature] Title (PR #N, commit-sha, N round CR APPROVED + Codex Phase 7 SHIP)
  - Main changes: ...
  - Review responses: ...
